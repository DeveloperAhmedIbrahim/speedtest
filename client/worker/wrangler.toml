name = "speedtest-backend"
main = "src/worker.js"
compatibility_date = "2026-01-01"

# No bindings, no storage, no secrets — this Worker is stateless on purpose.

# Optional: serve it from your own domain instead of *.workers.dev.
# The zone must already be on Cloudflare.
# routes = [
#   { pattern = "st.techrevivals.net", custom_domain = true }
# ]
